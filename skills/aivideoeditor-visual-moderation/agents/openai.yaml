interface:
  display_name: "AIVideoEditor Visual Moderation"
  short_description: "Aliyun review, Feishu API policy sync, ASR, and subtitle/audio redaction"
  default_prompt: "Use $aivideoeditor-visual-moderation to synchronize moderation keywords through the official Feishu Wiki/Docx APIs, submit videos to Aliyun Green, return concrete military, political, and NSFW violation names with hit timestamps, route local videos into 过了/没过 folders, expose allow_short_drama_editing so only passed videos enter later short-drama editing, generate higher-accuracy transcripts with open-source ASR when needed, or apply business-rule subtitle masking with synchronized audio muting."
